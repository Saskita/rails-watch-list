      <%= simple_form_for @list do |f| %>
        <%= f.input :name %>
        <%# <%= f.input :image, as: :file, label: "Picture:" %>

        <%# <%= f.submit "Create List", class: "btn border mt-3" %>
      <% end %>

