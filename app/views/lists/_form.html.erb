      <%= simple_form_for @list do |f| %>
        <%= f.input :name %>
        <%= f.input :photo, as: :file, label: "Picture:" %>
        <% button_text = @list.new_record? ? "Create List" : "Edit List"   %>
        <%= f.submit button_text, class: "btn border mt-3" %>

      <% end%>

